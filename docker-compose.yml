version: '3.8'

services:
  logger:
    build:
      context: ./src/services/logger
    volumes:
      - ./src/services/logger:/app
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO